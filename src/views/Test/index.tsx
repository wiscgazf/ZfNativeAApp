import React from 'react';
import { SafeAreaView, View, Text, ScrollView } from 'react-native';

const Test = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>这是测试</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Test;
