import React from 'react';
import { SafeAreaView, View, Text, ScrollView } from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>这是首页</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Home;
