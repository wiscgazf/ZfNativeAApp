import React from 'react';
import { SafeAreaView, View, Text, ScrollView } from 'react-native';

const Me = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>这是我的</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Me;
